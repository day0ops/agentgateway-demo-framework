apiVersion: v1
kind: Service
metadata:
  name: m2m-agent
  namespace: agentgateway-system
  labels:
    app.kubernetes.io/managed-by: agentgateway-demo
    agentgateway.dev/feature: m2m-agent
    app: m2m-agent
spec:
  selector:
    app: m2m-agent
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP
