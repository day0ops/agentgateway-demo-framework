apiVersion: v1
kind: Service
metadata:
  name: mcp-stock-server
  namespace: agentgateway-system
  labels:
    app.kubernetes.io/managed-by: agentgateway-demo
    agentgateway.dev/feature: mcp-stock-server
    app: mcp-stock-server
spec:
  selector:
    app: mcp-stock-server
  ports:
    - port: 8000
      targetPort: 8000
  type: ClusterIP
