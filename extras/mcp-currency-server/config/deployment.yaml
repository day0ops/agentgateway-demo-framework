apiVersion: apps/v1
kind: Deployment
metadata:
  name: mcp-currency-server
  namespace: agentgateway-system
  labels:
    app.kubernetes.io/managed-by: agentgateway-demo
    agentgateway.dev/feature: mcp-currency-server
    app: mcp-currency-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mcp-currency-server
  template:
    metadata:
      labels:
        app: mcp-currency-server
    spec:
      serviceAccountName: mcp-currency-server
      containers:
        - name: server
          image: mcp-currency-server:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "200m"
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 30
