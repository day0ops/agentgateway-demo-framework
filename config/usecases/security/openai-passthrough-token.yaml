apiVersion: agentgateway.demo/v1
kind: UseCase
metadata:
  name: openai-passthrough-token
  description: Passthrough token authentication for OpenAI — client provides the API key in the Authorization header
  docs: https://docs.solo.io/agentgateway/2.1.x/llm/api-keys/#passthrough

spec:
  diagram: |
    flowchart LR
      Client -->|Bearer API-key| AGW[AgentGateway]
      AGW -->|passthrough token| OpenAI[OpenAI]

  features:
    - name: providers
      config:
        providers:
          - name: openai
            providerName: openai
            pathPrefix: /openai
            model: gpt-4o-mini
            authMode: passthrough

  tests:
    - name: test-passthrough-with-valid-token
      description: Request succeeds when the client supplies a valid Bearer token
      steps:
        - action: send-request
          endpoint: /openai
          headers:
            Authorization: 'Bearer ${OPENAI_API_KEY}'
          prompt: 'What is 2+2?'
        - action: verify
          expect:
            status: success
            contains: '4'

    - name: test-passthrough-without-token
      description: Request fails when no Authorization header is provided
      steps:
        - action: send-request
          endpoint: /openai
          prompt: 'Hello'
        - action: verify
          expect:
            status: error
